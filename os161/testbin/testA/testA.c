#include <stdio.h>

int main() {
    int ret = printint(5);
    // printf("printint ret: %d\n", ret);

    ret = reversestring("hello", 5);
    // printf("reversestring ret: %d\n", ret);

    exit(0);
}